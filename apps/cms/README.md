# @porcupine/cms

Planned: Next.js + Payload CMS (admin UI + content API).

## Dev

From repo root:

```bash
npm install
npm --workspace @porcupine/cms run dev
```

MVP schema will include Pages, Offers, and Disclosures.
