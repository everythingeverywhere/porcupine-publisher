{
  "name": "porcupine-publisher",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm -ws test",
    "lint": "npm -ws lint",
    "build": "npm -ws build",
    "typecheck": "npm -ws typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
